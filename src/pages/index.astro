---
import Global from "../../packages/layouts/Global.astro";

import BaseHead from "../../packages/components/BaseHead.astro";
import { SITE_TITLE, SITE_DESCRIPTION, ROUTE_LABEL } from "@/packages/consts";

import { getBadges } from "@/packages/utils/badges";
import MantineMain from "../../packages/scaffold/MantineMain";
import BlogSections from "../components/BlogSections.astro";
import SpotifyPlayer from "../components/SpotifyPlayer";
import Tags from "@/packages/tags/Tags.astro";
import Footer from "@/packages/footer/Footer.astro";
import Header from "@/packages/header/Header.astro";
import MantineHeatWrapper from "@/packages/heat/MantineHeatWrapper.astro";
import WorldMap from "../components/WorldMap.astro";
import ParticleHero from "@/packages/particle-hero/ParticleHero.astro";
import {
  findNearestGalleryEntry,
  readGalleryTraceUrl,
} from "@/packages/utils/gallery";


export const prerender = true;
const badges = await getBadges();

const nearest = findNearestGalleryEntry();
const galleryImage = nearest
  ? {
      url: nearest.image,
      traceUrl: readGalleryTraceUrl(nearest.id),
      colorSet: nearest.colorSet,
    }
  : undefined;
---

<Global>
  <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} slot="head" />
  <Header />
  <section id="home-page" class="main">
    <MantineMain client:load>
      <ParticleHero galleryImage={galleryImage} />
      <MantineHeatWrapper />
      <BlogSections />
      <WorldMap />
      <Tags tags={badges} />
      <SpotifyPlayer client:visible />
      <!-- <NewsLetter /> -->
      <Footer slug={"index"} />
    </MantineMain>
  </section>
</Global>
