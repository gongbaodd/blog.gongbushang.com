
describe('extract.title', () => {
    it('should extract the title from post', () => {})

    it('should extract the title from slug', () => {})

    it('should throw error from an invalid post', () => {})

})

describe('extract.date', () => {
    it('should extract the date', () => {})
})