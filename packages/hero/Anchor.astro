---
import BlogSkeleton from "@/src/skeletons/blog";
import { ROUTE_LABEL } from "../consts";
import MantineAnchor from "./MantineAnchor.tsx";

const props = Astro.props;
---

<MantineAnchor href={props.href}>
  <slot />
  <section transition:name={ROUTE_LABEL.Blog} slot="loader">
    <BlogSkeleton />
  </section>
</MantineAnchor>
